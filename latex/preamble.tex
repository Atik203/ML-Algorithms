% ===========================================================================
% preamble.tex
% Machine Learning Laboratory Manual - shared packages and styling
%
% Builds with pdfLaTeX: the default latexmk recipe of VS Code LaTeX Workshop
% and Overleaf's default compiler both build this document with no extra
% configuration files.
% ===========================================================================

% --- Page geometry ----------------------------------------------------------
\usepackage[a4paper,top=2.5cm,bottom=2.5cm,left=2.5cm,right=2.5cm,headheight=15pt]{geometry}

% --- Fonts (pdfLaTeX; available in TeX Live and on Overleaf) ----------------
\usepackage[T1]{fontenc}
\usepackage{libertinus}               % Libertinus Serif / Sans text face
\usepackage[varqu,varl]{zi4}          % Inconsolata for code

% --- Line spacing -----------------------------------------------------------
\usepackage{setspace}
\setstretch{1.15}

% --- Typographic quality ----------------------------------------------------
\usepackage{microtype}

% --- Colors (academic blue theme) -------------------------------------------
\usepackage{xcolor}
\definecolor{mlblue}{HTML}{1F4E79}
\definecolor{mlblue2}{HTML}{2E75B6}
\definecolor{mlteal}{HTML}{0F7B8A}
\definecolor{mlgray}{HTML}{F4F6F8}
\definecolor{mlrule}{HTML}{C9D3DD}
\definecolor{mlgreen}{HTML}{2E7D32}
\definecolor{mlorange}{HTML}{C55A11}
\definecolor{mlred}{HTML}{A33A3A}
\definecolor{mlviolet}{HTML}{7C3AED}
\definecolor{mlcodeblue}{HTML}{1D4ED8}
\definecolor{mlamber}{HTML}{B45309}
\definecolor{mlcomment}{HTML}{3D7A57}

% --- Boxes ------------------------------------------------------------------
\usepackage[most]{tcolorbox}
\tcbuselibrary{listings,breakable,skins}
\tcbset{
  mlbox/.style={
    enhanced, breakable=false, colback=mlgray, colframe=mlblue,
    boxrule=0.6pt, arc=1.5mm, left=3mm, right=3mm, top=2mm, bottom=2mm,
    fontupper=\small
  },
  mlwarn/.style={
    enhanced, breakable=false, colback=mlorange!6, colframe=mlorange,
    boxrule=0.6pt, arc=1.5mm, left=3mm, right=3mm, top=2mm, bottom=2mm,
    fontupper=\small
  },
}

% Quick-facts box: \quickfacts{label}{value}{label}{value}{label}{value}
\newcommand{\quickfacts}[6]{%
  \begin{tcolorbox}[mlbox]
    {\sffamily\small
      \textbf{\color{mlblue}#1:} #2\\[0.7mm]
      \textbf{\color{mlblue}#3:} #4\\[0.7mm]
      \textbf{\color{mlblue}#5:} #6}
  \end{tcolorbox}
}

% Placeholder used while the manual is being filled
\newcommand{\placeholder}{%
  \begin{tcolorbox}[mlwarn]
    \textcolor{mlorange}{\textit{Content, result plots and complete code listings for this
    experiment are filled in after the format review.}}
  \end{tcolorbox}
}

% --- Math -------------------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{libertinust1math}   % matching math font (load after amssymb)

% --- Code listings ----------------------------------------------------------
\usepackage{listings}
\lstdefinestyle{mlcode}{
  language=Python,
  basicstyle=\ttfamily\fontsize{8.4}{10.2}\linespread{1}\selectfont,
  keywordstyle=\color{mlviolet}\bfseries,
  keywordstyle=[2]\color{mlteal},
  keywordstyle=[3]\color{mlcodeblue}\bfseries,
  commentstyle=\color{mlcomment}\itshape,
  stringstyle=\color{mlamber},
  morekeywords=[2]{print,len,range,enumerate,zip,map,filter,sum,min,max,abs,round,
    sorted,reversed,isinstance,type,int,float,str,bool,list,dict,set,tuple,
    open,super,self,format,setattr,getattr,hasattr},
  morekeywords=[3]{np,pd,plt,sns,torch,nn,optim,keras,sklearn},
  breaklines=true,
  breakatwhitespace=false,
  breakindent=8pt,
  postbreak=\mbox{\textcolor{gray}{$\hookrightarrow$}\space},
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  upquote=true,
  tabsize=4,
  aboveskip=0pt,
  belowskip=0pt,
  numbers=left,
  numberstyle=\tiny\color{mlblue!55},
  numbersep=7pt,
  xleftmargin=16pt,
}
\lstset{style=mlcode}
\renewcommand{\lstlistingname}{Listing}

% Breakable, titled code box used for every code listing
\newtcblisting{codebox}[1][]{
  enhanced, breakable,
  listing engine=listings,
  listing only,
  colback=mlgray, colframe=mlblue!70,
  borderline west={1.1mm}{0pt}{mlblue!55},
  boxrule=0.5pt, arc=1.2mm,
  left=2.4mm, right=1.5mm, top=1.5mm, bottom=1.5mm,
  title=, coltitle=white, colbacktitle=mlblue,
  fonttitle=\small\sffamily\bfseries,
  toptitle=0.8mm, bottomtitle=0.8mm,
  #1
}

% --- Tables (full-grid style: \hline on every row) --------------------------
\usepackage{longtable}
\usepackage{array}
\usepackage{colortbl}
\usepackage{etoolbox}
\AtBeginEnvironment{longtable}{\small\linespread{1}\selectfont}
\AtBeginEnvironment{tabular}{\small\linespread{1}\selectfont}
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}
\setlength{\tabcolsep}{5pt}
% highlight cell for the best value
\newcommand{\best}[1]{\cellcolor{mlgreen!12}\textbf{#1}}
% light blue header row for tables
\newcommand{\tablehead}[1]{\rowcolor{mlblue!8}#1}

% --- Contents and list styling ----------------------------------------------
\usepackage{titletoc}
\titlecontents{part}[0em]
  {\addvspace{14pt}\large\bfseries\color{mlblue}}
  {Part \thecontentslabel\enspace}{}
  {\hfill\contentspage}
\titlecontents{chapter}[1.9em]
  {\addvspace{3pt}\small}
  {\color{mlblue}\bfseries Experiment \thecontentslabel\enspace}
  {\hspace*{-1.9em}}
  {\titlerule*[0.6pc]{.}\contentspage}
\titlecontents{figure}[2.4em]
  {\small}
  {\thecontentslabel\enspace}{}
  {\titlerule*[0.6pc]{.}\contentspage}
\titlecontents{table}[2.4em]
  {\small}
  {\thecontentslabel\enspace}{}
  {\titlerule*[0.6pc]{.}\contentspage}

% --- Headings ---------------------------------------------------------------
\usepackage{titlesec}
% Mini table of contents printed on every part-opening page
\newcommand{\parttoc}{%
  \begingroup
  \raggedright\normalfont\normalsize
  \noindent{\color{mlblue}\sffamily\bfseries Experiments in this Part}\break
  \vspace{2mm}%
  \small
  \startcontents[parts]%
  \printcontents[parts]{}{0}{}%
  \endgroup}
\titleformat{\part}[display]
  {\centering\normalfont\Huge\bfseries\color{mlblue}}
  {\partname~\thepart}
  {10pt}
  {\Huge}
  [\vspace{7mm}\parttoc]
\titlespacing*{\part}{0pt}{30pt}{18pt}
% experiments run continuously (no page break before each chapter)
\titleclass{\chapter}{straight}[\part]
\titleformat{\chapter}[display]
  {\normalfont\Large\bfseries\color{mlblue}}
  {\normalsize\sffamily\bfseries\color{mlblue2}\chaptername~\thechapter}
  {8pt}
  {\Large}
  [\vspace{3pt}{\color{mlrule}\hrule height 0.9pt}]
\titlespacing*{\chapter}{0pt}{-10pt}{14pt}
\titleformat{\section}{\large\bfseries\color{mlblue}}{}{0em}{}
\titlespacing*{\section}{0pt}{10pt}{4pt}
\titleformat{\subsection}{\normalsize\bfseries\color{mlblue2}}{}{0em}{}
\titlespacing*{\subsection}{0pt}{8pt}{3pt}
% parts and chapters are numbered (chapters are "Experiment N"); sections are not
\setcounter{secnumdepth}{0}
\renewcommand{\chaptername}{Experiment}
% Dedicated running title (independent of LaTeX's volatile section marks)
\newcommand{\mlrunningtitle}{}
\newcommand{\runninghead}[1]{\gdef\mlrunningtitle{#1}}
\renewcommand{\chaptermark}[1]{\runninghead{\chaptername~\thechapter: #1}}
\renewcommand{\sectionmark}[1]{}

% --- Headers / footers ------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\sffamily\color{mlblue}Machine Learning Laboratory Manual}
\fancyhead[R]{\small\sffamily\color{mlblue}\nouppercase{\mlrunningtitle}}
\fancyfoot[C]{\small\sffamily\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\fancypagestyle{plain}{%
  \fancyhf{}\fancyfoot[C]{\small\sffamily\thepage}\renewcommand{\headrulewidth}{0pt}}

% --- Lists, figures, floats, links ------------------------------------------
\usepackage{enumitem}
\setlist{itemsep=2pt,topsep=3pt}
\usepackage{graphicx}
\usepackage{caption}
\captionsetup{font=small,labelfont={bf,color=mlblue},skip=4pt}
\usepackage{placeins}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.7}
\usepackage[hidelinks,bookmarksnumbered,bookmarksopen,bookmarksopenlevel=0]{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=mlblue,
  urlcolor=mlteal,
  citecolor=mlblue,
  pdftitle={Machine Learning Laboratory Manual - Completed Edition},
  pdfauthor={Dr. Ohidujjaman Tuhin}
}

% --- Pandoc fragment compatibility (not required for hand-written files) ----
\providecommand{\passthrough}[1]{#1}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\LTcaptype}{table}

% --- Misc -------------------------------------------------------------------
\setlength{\parskip}{4pt}
\setlength{\parindent}{0pt}
\setlength{\emergencystretch}{1.5em}   % avoid minor overfull lines
\clubpenalty=1000
\widowpenalty=1000
\displaywidowpenalty=1000
\raggedbottom
